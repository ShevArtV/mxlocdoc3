mxLocDoc
========

mxLocDoc is a lightweight MODX Revolution 3 extra for reading local Markdown
documentation inside the MODX manager.

The package includes a manager CMP, manifest navigation, safe Markdown rendering,
protected asset delivery, multilingual documentation roots and search over local
.md files.
