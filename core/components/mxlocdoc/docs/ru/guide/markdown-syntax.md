---
title: Синтаксис Markdown
order: 4
---
# Синтаксис Markdown

Документы пишутся на **Markdown** — простой разметке, где оформление задаётся
обычными символами прямо в тексте. mxLocDoc отображает их через Parsedown в
безопасном режиме. Ниже — что поддерживается, что нет, и короткая шпаргалка для
тех, кто с Markdown раньше не работал.

Полный справочник: [Markdown Guide](https://www.markdownguide.org/basic-syntax/)
(разделы «Basic Syntax» и «Extended Syntax»). Интерактивный тур —
[CommonMark](https://commonmark.org/help/).

## Что поддерживается

### Заголовки

````markdown
# Заголовок первого уровня
## Второго уровня
### Третьего уровня
````

### Выделение текста

````markdown
**жирный**, *курсив*, ~~зачёркнутый~~
````

### Списки

````markdown
- пункт
- пункт
  - вложенный пункт

1. первый
2. второй
````

### Ссылки и картинки

````markdown
[текст ссылки](guide/setup.md)
![подпись под картинкой](images/diagram.svg)
````

Относительные ссылки на `.md`-файлы открываются внутри mxLocDoc, а относительные
картинки отдаются через защищённый connector: mxLocDoc проверяет путь, расширение
и размер файла и не выпускает запрос за пределы корня документации. Внешние ссылки
и картинки остаются обычными.

### Код

Внутри строки — в одинарных обратных кавычках: `` `пример` ``. Блоком — в тройных
обратных кавычках, можно указать язык:

`````markdown
```php
echo 'hello';
```
`````

Код показывается моноширинным шрифтом. Подсветка синтаксиса не выполняется.

### Цитаты и разделитель

````markdown
> цитата

---
````

### Таблицы

````markdown
| Колонка | Значение |
|---------|----------|
| A       | 1        |
| B       | 2        |
````

## Что не поддерживается

- **Списки задач** `- [ ] пункт` / `- [x] пункт` — покажутся как обычный текст со
  скобками, без чекбоксов.
- **Сноски** `[^1]`, **списки определений**, **аббревиатуры**, **инлайн-атрибуты**
  `{.класс}` — это расширения ParsedownExtra, которых в пакете нет.
- **Сырой HTML** (`<div>`, `<iframe>`, `<details>` и т.п.) — намеренно
  экранируется и показывается как текст. Это защита от небезопасного содержимого.
- **Подсветка синтаксиса** в блоках кода — код моноширинный, но не раскрашивается.
- Небезопасные ссылки (`javascript:` и подобные) вырезаются.

## Шпаргалка

Слева — как написать, справа — как это выглядит после отображения.

| Что нужно      | Как написать          | Результат                     |
|----------------|-----------------------|-------------------------------|
| Заголовок      | `## Название`         | заголовок раздела             |
| Жирный текст   | `**текст**`           | **текст**                     |
| Курсив         | `*текст*`             | *текст*                       |
| Зачёркнутый    | `~~текст~~`           | ~~текст~~                     |
| Список         | `- пункт`             | маркированный список          |
| Нумерация      | `1. пункт`            | нумерованный список           |
| Ссылка         | `[текст](адрес)`      | [текст](https://modx.com)     |
| Код в строке   | `` `код` ``           | `код`                         |
| Цитата         | `> текст`             | блок цитаты                   |

Совет: оставляйте пустую строку между абзацами, заголовками и списками — без неё
блоки могут «слипнуться» в один абзац.
